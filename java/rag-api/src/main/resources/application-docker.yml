spring:
  data:
    redis:
      host: redis
  rabbitmq:
    host: rabbitmq
    publisher-confirm-type: correlated
  datasource:
    url: jdbc:p6spy:mysql://mysql:3306/rag_knowledge?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver

minio:
  endpoint: http://minio:9000

ai-service:
  python-base-url: http://python-ai:8000
  callback-token: ${AI_SERVICE_CALLBACK_TOKEN:}
  internal-token: ${AI_SERVICE_INTERNAL_TOKEN:${AI_SERVICE_CALLBACK_TOKEN:}}
